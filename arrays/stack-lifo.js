const stack = [1, 2, 3, 4, 5];

stack.push(6); //adds 6 in the end of the stack
console.log(stack);


//removes the last element
const topElement = stack.pop();
console.log("Popped Element:", topElement);
console.log(stack);
